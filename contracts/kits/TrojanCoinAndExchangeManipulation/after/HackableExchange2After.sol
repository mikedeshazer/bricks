pragma solidity ^0.6.6;
import "../../../../contracts/interfaces/basic/ERC20.sol";
import "../../../../contracts/interfaces/basic/safemath.sol";

contract HackableExchange2After{
/*
  mapping(address => uint256) positions;

    address oracleAddress;

  function createPosition(address tokenCreatingPosition, address tokenPayingWith, bool long, uint256 marginTimes, uint256 amountFromToken){
    positions[msg.sender] = amountFromToken;
  }

  function setOracleAddress(address newAddress){
    oracleAddress = newAddress;
  }


  function closePosition(){

    ERC20 exchangeUsingToGetRate = ERC20(oracleAddress);
    uint256 positionAmount = positions[msg.sender];


  }
*/
}
